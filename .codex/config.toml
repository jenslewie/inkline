[agents]
enabled = true
# Temporary fallback for https://github.com/openai/codex/issues/35097.
# Restore gpt-5.6-luna / max after Luna is accepted as a Multi-Agent V2 child.
default_subagent_model = "gpt-5.6-terra"
default_subagent_reasoning_effort = "high"

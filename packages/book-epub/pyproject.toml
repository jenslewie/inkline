[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "book-epub"
version = "0.1.0"
description = "CanonicalDocument to EPUB exporter."
requires-python = ">=3.11"
dependencies = ["book-canonical>=0.1.0"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
